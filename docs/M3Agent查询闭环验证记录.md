# M3 Agent 查询闭环验证记录

验证日期：2026-08-24

## 当前结论

M3 已完成。重建后端运行环境、重新配置对话和嵌入模型、导入语义知识并发布 Agent 后，完整真实模型链路达到核心问题 `20/20`、多轮下钻 `3/3`、意图识别 `4/4`，最终证据 `summary.passed=true`。

最终证据：`demo/enterprise-finance/m3/evidence/final-accepted.json`

## 实现内容

- 复用现有 StateGraph 查询链路，覆盖意图识别、知识召回、计划生成、SQL 生成与执行、结果分析、报告和图表。
- 强化报告提示词：直接回答问题，并展示指标聚合方式、日期字段、时间范围、组织或客户归属、状态和排除条件、实际表及关联字段。
- 在 `ReportGeneratorNode` 增加确定性兜底：模型返回空流时保留真实结果；缺少依据小节时，根据执行计划和实际 SQL 自动补齐。
- 保留模型流中的空白片段，避免流式 Markdown 标题中的空格被过滤。
- 在 `QueryEnhanceNode` 增加空输出和不可解析输出兜底，继续使用原始问题执行查询。
- 增加 20 道核心问题、3 道多轮下钻问题和 4 道意图问题。
- 增加流式验收程序，保存节点顺序、attempt、召回证据、SQL、结果集样例、报告、图表、重试消息和最终回答。
- 增加 HTML 导出验证、定向复跑和最佳进度证据合并能力。

## 运行环境重建

- 后端重新启动并通过模型就绪检查。
- 新建并发布 Agent `5`，绑定数据源 `4`，初始化 13 张合成业务表。
- 导入 63 个字段语义、14 条业务知识和 18 组标准问答；业务知识与标准问答均完成向量化。
- 5 组知识召回校验全部通过。

Agent 和数据源 ID 来自本次 H2 内存管理库，后续重启可能变化；可重复入口以 `demo/enterprise-finance/` 下的脚本和数据文件为准。

## 真实端到端结果

| 验收项 | 结果 |
|---|---:|
| 核心问题完整报告闭环 | 20/20 |
| 多轮下钻完整通过 | 3/3 |
| 意图识别 | 4/4 |
| 包含图表的运行 | 10 |
| HTML 导出 | 通过，`text/html;charset=UTF-8`，8911 bytes |
| 总体验收 | `summary.passed=true` |

最终证据由单次完整的新运行实例评测产物生成，不依赖旧运行实例补齐成功项。部分模型流将 Markdown 标题拆成 `##`、空格和标题文本三个片段，评测器因此同时接受 `## 数据口径与查询依据` 和语义等价的紧凑流式形式；相关运行均具有非空报告、实际 SQL、结果集和查询依据内容。

本次完整复跑没有触发 SQL 重试。有限重试路径由 `SqlGenerateNode` 自动化测试覆盖；此前真实轨迹也观察到结构校验失败后携带原因进入第 2 次尝试并成功执行。

## 自动化验证

```powershell
.\mvnw.cmd -pl data-agent-management test
python -m unittest discover -s demo/enterprise-finance/m3 -p "test_*.py" -v
```

结果：

- 后端 `1716/1716` 通过，0 failures、0 errors、0 skipped，Checkstyle 0 violations。
- M3 评测逻辑测试 `2/2` 通过。
- 报告节点、查询增强节点和提示词定向测试 `35/35` 通过。
- 报告测试覆盖空模型流兜底和流式空白片段保留；查询增强测试覆盖空输出和不可解析输出兜底。

## 里程碑判定

路线图要求的功能、自动化测试、真实端到端场景、失败路径和证据文档均已完成，M3 状态更新为 `completed`，可以进入 M4 SQL 安全与权限阶段。

## 边界

- 所有问题和结果仅来自固定种子的合成数据库，不是生产数据或真实业务成果。
- M3 完成“能查询、能解释、能下钻、能出报告”的闭环；只读账号、SQL AST 白名单、数据权限、脱敏、超时、行数限制和审计属于 M4。
- 证据 JSON 包含问题、SQL 和合成结果，不包含 API Key 或数据库密码。
