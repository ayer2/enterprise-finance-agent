# 3–5 分钟演示脚本

## 演示前检查

- Compose 三个服务均为 healthy，`deploy/verify.ps1` 输出 `M7_DEPLOYMENT_VERIFICATION_PASSED`。
- Chat、Embedding 和 Agent 均已激活，业务数据源使用 `enterprise_agent_ro`。
- 浏览器缩放为 100%，关闭任何可能显示 API Key、数据库密码或个人账号的页面。
- 预先准备一个成功会话，避免外部模型偶发波动影响完整演示。

## 00:00–00:35 项目定位

打开经营分析首页。说明这是基于 Spring AI Alibaba DataAgent 的个人二次开发，只使用固定种子合成数据；不是公司生产系统，也不是从零开发上游框架。

展示首页的 Agent、模型就绪、只读数据源、业务表和推荐问题状态。

## 00:35–01:40 完整查询闭环

提交：`2025年全年销售额是多少？`

等待流式执行时依次指出：意图识别、知识召回、Schema、计划、SQL、语义一致性、只读执行和报告生成。打开“执行依据”，展示指标口径、召回证据和 SQL，而不是只展示最终自然语言答案。

指出实际合成结果 `95,165,908.34`，并说明只有 `APPROVED` 订单计入销售额。

## 01:40–02:25 多轮下钻与报告

在同一会话追问：`请按月拆分这笔全年销售额，并列出每月金额。`

展示多轮上下文、12 个月结果和图表。点击 Markdown 或 HTML 下载，说明报告来源于本次查询结果，并包含口径和依据。

## 02:25–03:15 安全边界

展示架构与安全设计中的纵深防御：AST 单条 SELECT、表字段白名单、部门范围、脱敏、500 行、15 秒、只读数据库账号和审计日志。

说明 M6 的 12 条危险 SQL 全部拦截；两道应收明细题因为 `receivable_no` 未加入白名单而失败，项目保留了失败证据，没有为提高分数绕过安全规则。

## 03:15–04:05 可重复评测

打开 M6 可读报告，展示四项实际结果：SQL 94.44%、结果 94.44%、口径 87.50%、危险请求 100%。强调这些数值只对应固定题集和合成数据，不是生产 SLA。

## 04:05–04:35 交付与边界

展示 README 的 Docker Compose 启动命令、无密钥 `.env.example`、架构/ER/时序图和已知限制。最后总结个人完成的增量：合成数据、语义资产、安全审计、经营页面、自动评测和容器化交付。
