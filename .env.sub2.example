# 复制为服务器上的 .env.sub2，填写真实值后再启动。
SUB2_AGENT_MYSQL_ROOT_PASSWORD=CHANGE_ME
SUB2_AGENT_BACKEND_PORT=8066
SUB2_AGENT_FRONTEND_PORT=3001
# 不要直接照抄占位值；服务器上先执行 docker inspect sub2api-postgres 获取网络名。
# SUB2_UPSTREAM_NETWORK=sub2api_sub2api-network
